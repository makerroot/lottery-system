# ===== 必须配置的安全参数 =====
# JWT 密钥（必须设置为强随机值）
# 生成方法: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production

# ===== 服务器配置 =====
# 服务器端口
SERVER_PORT=8080

# ===== 数据库配置 =====
# 数据库连接字符串
# SQLite: lottery.db
# MySQL: user:password@tcp(localhost:3306)/lottery?charset=utf8mb4&parseTime=True&loc=Local
DATABASE_URL=root:password@tcp(localhost:3306)/lottery_db?charset=utf8mb4&parseTime=True&loc=Local

# ===== CORS 配置 =====
# 允许的跨域来源（逗号分隔）
# 生产环境不要使用 *，应该指定具体域名
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# ===== Redis 配置 =====
# 是否启用 Redis 限流（true/false）
# 启用后可以实现分布式限流，多个实例共享限流状态
ENABLE_REDIS=false

# Redis 地址（格式：host:port）
REDIS_ADDR=localhost:6379

# Redis 密码（如果 Redis 设置了密码）
REDIS_PASSWORD=

# Redis 数据库编号（0-15）
REDIS_DB=0
